<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="template_city" name="complementos" inherit_id="l10n_pe_website_sale.partner_address_info">
        <!-- <xpath expr="//t[@t-name='l10n_pe_website_sale.partner_address_info']" position="replace">

        <div t-attf-class="mb-3 #{error.get('city_id') and 'o_has_error' or ''} col-lg-6 div_city_id" >
            <label class="col-form-label" for="city_id">City</label>
            <select id="city_id" name="city_id" t-attf-class="form-select #{error.get('city_id') and 'is-invalid' or ''}" data-init="1">
                <t t-foreach="state_cities" t-as="city">
                    <option t-att-value="city.id" t-att-selected="city.id == ('city_id' in checkout and checkout['city_id'] != '' and int(checkout['city_id']))">
                        <t t-out="city.name"/>
                    </option>
                </t>
            </select>
        </div>

        <div t-attf-class="mb-3 #{error.get('l10n_pe_district') and 'o_has_error' or ''} col-lg-6 div_district">
            <label class="col-form-label" for="l10n_pe_district">District</label>
            <select id="l10n_pe_district" name="l10n_pe_district" t-attf-class="form-select #{error.get('l10n_pe_district') and 'is-invalid' or ''}" data-init="1">
                <t t-foreach="city_districts" t-as="district">
                    <option t-att-value="district.id" t-att-selected="district.id == ('l10n_pe_district' in checkout and checkout['l10n_pe_district'] != '' and int(checkout['l10n_pe_district']))">
                        <t t-out="district.name"/>
                    </option>
                </t>
            </select>
        </div>            
        </xpath> -->
    </template>

</odoo>
